echo "🚧 Running static analyses..."
if node -e "const [major]=process.versions.node.split('.').map(Number); process.exit(major>=24?0:1)"; then
  npx lint-staged
  echo "✅ Static analyses completed."
  exit 0
fi

echo "Skipping lint-staged: Node >=24 required"
